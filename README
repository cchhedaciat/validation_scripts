1. Add student to adm group
2. Make journal persistent
3. Read access to root homedir and .bash_history
4. Add export PROMPT_COMMAND="history -a; history -c; history -r; $PROMPT_COMMAND" in users .bashrc
